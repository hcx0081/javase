package com.javase.sort;

/**
 * @Description:
 */
public class MergingSort {
    int[] mergingSort(int[] arr) {
        int lowIndex = 0;
        int highIndex = arr.length - 1;
        int midIndex = (lowIndex + highIndex) / 2;
        return new int[]{-1};
    }
}
